# Render Blueprint - Infrastructure as Code
# Deploy with: https://render.com/deploy

services:
  # Backend API
  - type: web
    name: demony-api
    env: node
    region: oregon
    plan: free
    buildCommand: cd packages/backend && npm install --production
    startCommand: cd packages/backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: RENDER
        value: "true"
      - key: RENDER_EXTERNAL_URL
        value: https://demony-api.onrender.com
      - key: PORT
        value: 3001
      - key: DATABASE_URL
        sync: false  # Set manually in Render dashboard
      - key: JWT_SECRET
        generateValue: true
      - key: WEB_URL
        value: https://demony-web.onrender.com
    healthCheckPath: /

  # Keep-Alive Cron Job - Pings the API every 14 minutes to prevent sleep
  - type: cron
    name: demony-keepalive
    env: node
    region: oregon
    plan: free
    schedule: "*/14 * * * *"  # Every 14 minutes
    buildCommand: echo "Keep-alive job ready"
    startCommand: curl -s https://demony-api.onrender.com/health || echo "Ping sent"

  # Web Frontend (Static Site)
  - type: web
    name: demony-web
    env: static
    buildCommand: cd packages/web && npm install && npm run build
    staticPublishPath: packages/web/dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=31536000
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://demony-api.onrender.com/api
